# 项目文档入口与维护记录

更新：2026-09-09。仅支持 League Akari 内的原生 `member-analysis`；本文件不是全项目验收通过声明。

## 1. 按问题找文档

| 需要了解什么     | 当前入口                                                                  | 维护范围                                     |
| ---------------- | ------------------------------------------------------------------------- | -------------------------------------------- |
| 产品边界与权限   | [根 AGENTS](../../../AGENTS.md)                                           | 原生入口、本地数据、不自行发布或提交         |
| 工程流程与目录   | [Akari AGENTS](../../AGENTS.md)                                           | 开发入口、测试原则、按需读取 Skill           |
| UI 与产品设计    | [设计规范](design-spec.md)                                                | 风格、尺寸、布局、交互、文案、图表与视觉验收 |
| 接口和数据兼容   | [数据契约](member-analysis-contract.md)                                   | 版本、允许调用、代码定义位置                 |
| 修复、升级与验证 | [维护手册](member-analysis-maintenance-runbook.md)                        | 按影响范围测试、迁移、打包和清理             |
| 内部包与未来发布 | [发布规则](release-policy.md)                                             | 产品身份、手动更新、签名及发布前条件         |
| 上游修改预算     | [patch-manifest.json](patch-manifest.json)                                | 精确基线、接入点和预算；不手抄“当前 9/9”     |
| 过去的实现与验收 | [历史记录](history-through-2026-09-06.md)                                 | 原始证据，只按发生日期解释                   |
| 初始架构方案     | [早期技术设计](../../../LEAGUE_AKARI_MEMBER_ANALYSIS_TECHNICAL_DESIGN.md) | 背景与方案追溯，不作为当前功能清单           |

Skill 位于 `.agents/skills/`，负责具体执行方法，不重复维护一整套产品规范。
上游 README、CHANGELOG、旧公告和 native 说明保留其上游/历史用途，不代表本 fork 支持平台或发布就绪。
`CLAUDE.md` 只指向 AGENTS；退役网页目录只保留退役边界。

## 2. 发生冲突时

- 按当前任务指令和适用的 AGENTS 约束执行；各专门文档只负责表中的范围。
- 代码和测试说明“目前做到了什么”，不能覆盖“必须做到什么”。两者不一致时修正文档中的旧事实，或记录实现缺口，不能擅自放宽要求。
- 历史记录、早期技术设计及上游宣传/发布说明不覆盖现行规范。
- 同一条规则只在所属文档维护，其他地方放链接。当前状态和测试数量只在有日期的维护条目记录。

## 3. 已知验收范围

- 2026-08-01 的阶段测试是历史证据，不等于当前工作区所有改动均已验证或没有缺陷。
- 2026-08-26 整改记录仍有八主题人工视觉确认及当时 Python 测试未运行的说明；具体以历史条目及后继验证为准。
- 2026-09-06 被抓统计记录包含本地测试与构建结果；截图仅覆盖旧数据“需刷新”，有事件数据的最终视觉确认仍待完成。
- 本次整理不重新运行应用，不重新核定上述功能状态。当前发布仍只限已有本地/内部手动更新政策；公开发布条件见发布规则。

## 4. 文档维护方式

普通任务不新建完成报告。只在这里追加“日期、范围、关键变更、实际检查、未覆盖项”。
无需每次复述产品边界、工具版本或过去的测试数。只有相关规则改变才更新其所属文档。
长期技术设计、Skill、contract、runbook 可以独立成文，但必须有明确用途和入口。
历史记录不改写当时事实；确需更正时追加勘误并指明原条目。

## 5. 维护记录

### 2026-09-09 — `v0.2.0` 稳定版与 CCB 应用内更新

- 用户明确接受无 PFX 的首个公开稳定版及其 SmartScreen 风险。版本改为 `0.2.0`；发布 workflow 生成稳定 GitHub Release，并随 CCB Windows x64 7z 上传 SHA-256 sidecar。
- 本版记录的官方 `dev` baseline 为 `5109b2f7fcce6e02312e534cd1729ed0f2142b51`。
- 首次稳定 workflow 已完成测试、构建、打包和发布；发布后 API 核对发现 GitHub 会强制将资产名中的空格规范化成点，API 重命名同样无法保留空格。点分隔名称因此确立为 canonical 格式，客户端、workflow 与 checksum 同步修正；不可更新的首次 Release 在公开确认前删除，并从修正后的同版本 tag 重新执行完整门禁。
- 更新检查不再读取官方 Akari release API，只接受 `kekekawaii2839/LeagueAkari-CCB` 的最新稳定 GitHub Release、精确 tag/文件名/下载路径及 checksum 文件。下载内容在退出替换前流式校验 SHA-256，失败即删除；更新后启动 `LeagueAkariCCB.exe`。便携版不调用可能影响官方协议注册的卸载路径。
- 该通道仍未签名，SHA-256 sidecar 与包同源，不能声称独立真实性、原子切换或失败回滚。签名与 rollback-safe 状态仍按发布规则作为后续加固项。

### 2026-09-09 — 高级筛选日期占位与版本排序

- 五排分析的开始、结束日期输入显式关闭 Naive UI 文本占位，避免“请输入”覆盖浏览器的“年/月/日”日期提示；保留原生日期输入与现有无障碍名称。
- 版本选项改用数字感知的版本降序，`14.10` 会排在 `14.9` 之前，不再按字典序比较；新增日期渲染和多段版本边界测试。
- 两项定向测试与 `typecheck:web` 通过。为恢复仓库 Yarn 可执行文件链接执行了不可变依赖安装，链接完成但本机仍因缺少 Visual Studio C++ toolset 无法重建既有 `better-sqlite3` native postinstall；本次渲染器检查不依赖该 native 构建。未做原生 Electron 或八主题截图，本次只改变空日期占位及下拉顺序。

### 2026-09-08 — 发布仓库、手动更新源与上游自动同步

- CCB 仓库身份固定为 `kekekawaii2839/LeagueAkari-CCB`；应用项目链接、包元数据与手动下载来源均指向该仓库，不再把官方仓库作为 CCB 下载源。自动安装仍按发布规则保持编译期禁用，不能用未签名包绕过哈希、签名与回滚门槛。
- 新增每日及手动触发的 upstream sync：在临时分支合并官方 `Hanxven/LeagueAkari:dev`，更新精确 upstream baseline，依次通过 patch guard、全量测试、production build、Windows CCB 打包与 artifact scan 后才推入 fork `dev`；冲突或任一 gate 失败都不改动 `dev`。
- 移除继承自官方仓库、会在每次 `dev` push 发布官方命名包的 workflow；CCB 发布 workflow 只在显式 tag 或手动触发时构建 Windows x64 portable 包，tag 才创建 GitHub Release。`.phase7-package` 加入忽略，避免本机构建产物进入仓库。
- 首次发布准备已提交并推送到私有仓库 `dev`（首个远端功能提交 `1f66c5f`）。GitHub API 已确认默认分支为 `dev`，`Sync Official Upstream` 与 `Build CCB Release` 两个 workflow 均为 active；普通 `dev` push 只登记而不发布 CCB 包。数据泄漏守卫、patch guard（10/10、85 行）、YAML 解析、差异空白、13 项 self-update 定向测试以及 node/web typecheck 通过。全量 Vitest 在本机无结果输出后中止；production build 因正在运行的 Electron 占用 `out/preload/index.js` 而失败，不把这两项记为通过。
- 首次真实 upstream rehearsal 发现官方 `dev` 已前进到 `f31c1cd27f8eb131abe4f74e5b89d7adaf1949ed`；远端 workflow 的首次真实运行以 failure 停在合并 gate，准确保护了 fork `dev`。冲突位于 `AGENTS.md`、`package.json`、main bootstrap 和 renderer shard registry。临时分支手动解析后通过更新后 patch guard（10/10、83 行）、数据泄漏守卫、node/web typecheck，以及 21 文件/119 项 member-analysis 与 self-update 定向测试（1 文件/1 项跳过）。PnP 方式的全量测试为 111 文件/609 项通过、1 文件/1 项跳过，另 8 个 suite 因 PnP 严格依赖/Electron 安装环境失败；production build 也因 PnP peer 解析失败。因此尚未把该上游版本集成到发布分支，不能把首次冲突同步记为通过。
- 用户确认按兼容方案解析上述冲突并将候选版本定为 `0.2.0-ccb.1`：保留 fork 工程规则与 CCB 身份，采用官方 Yarn 4.18.0 和依赖升级；main bootstrap 同时注册官方 `MainWindowUiMain`、`NetworkMain` 与 `MemberAnalysisMain`，renderer 同时注册官方 `NetworkRenderer` 和 `MemberAnalysisRenderer`。声明 `yaml` 测试依赖后，更新后的 patch guard 为 11/11、89 行，数据泄漏守卫覆盖 1686 个 tracked/candidate 文件，node/web typecheck、全量 120 个测试文件（119 通过、1 跳过；677 项通过、1 跳过）和 production build 均通过；build 仅保留上游 `@reference` 与大 chunk 警告。本机普通依赖安装因缺少 Visual Studio C++ toolset 无法重建 `better-sqlite3`，测试与 build 使用完成链接但跳过 native build 的依赖树；Windows Release 仍必须通过 GitHub runner 的完整安装、打包和 artifact scan 才可发布。
- GitHub Windows runner 的发布演练依次暴露并修复三个 CI/打包问题：Node 默认 2 GiB heap 令 web typecheck OOM；`.gitignore` 的 `*.py[cod]` 误排除 PyInstaller 的 `.pyd` 运行库且 Windows checkout 会改变受清单保护的 SQL 字节；artifact uploader 默认忽略 `.phase7-package` 隐藏目录。现为两个验证 workflow 配置 4 GiB heap，精确保留 Python bytecode 排除但纳入八个 `.pyd`，用 `.gitattributes` 固定 worker 字节，并显式允许上传隐藏目录。最终 tag workflow run `34372255452` 在干净 Windows runner 上通过完整依赖安装、677 项测试（1 跳过）、production build、Windows x64 7z 打包、58 个 worker hash 与 143 个包内文件扫描，并成功创建 Release 资产。

### 2026-09-08 — 英雄池真实数据自适应排版

- 根据用户再次反馈，撤销前次跨列等高：每个分路独立排列，内容少时自然收紧，展开不会抬高其他分路。标题/成员/英雄文字分别统一为 15/14/13px，辅助文字 12px，英雄行 28px、头像 20px、圆环 64px；单英雄列表与圆环居中，更多按钮不参与圆环居中计算。按用户追加反馈，圆心数字与“场”统一为 SVG 14px / 400 字重（实际约 12.4px），取消过大的粗体数字。用户追加要求直接显示英雄胜率：每行保留右侧选用率，并在名称下明确显示“胜率 xx%”，不需要打开详情；两者不混用、不默认套胜负颜色。
- 本机实际 Electron 已验证新布局生效；真实数据包含不同数量的成员与英雄，检查外层无横向溢出，展开一列不改变其他列高度，随后恢复收起。只输出布局计数，不保存或展示真实身份、对局内容。
- 增加 `UnevenHeroes` 合成场景，覆盖偏固定分路、长名称及 1–8 英雄的不均匀分布。图集使用独立 `akari-hero-natural` 目录；照片占位仍属于合成预览限制。`typecheck:web` 通过，定向测试 106 通过、1 跳过；最终八主题 × 两种窗口的展开、环段选中、焦点恢复与外层无溢出检查通过。数据泄漏守卫和差异空白检查通过。视觉仍待用户确认。

### 2026-09-08 — 英雄池紧凑版样板

- 按用户确认收紧排版：成员上下间距从 20px 降为 12px，英雄行从 32px 降为 28px，场数/英雄数/前三占比合并一行；保留分路分栏与共享行对齐。更多英雄列表限制展开高度并可滚动，避免单列过长撑空其他列。
- 圆环从 56px 调为 64px、环线减细；小分段缝隙随短弧缩小，不吞掉小比例。各圆环对齐前三行，选中英雄同步加重对应环段；其他英雄对应合并的其他段，不改变实际比例或编造类别颜色。详情沿用上次已调整的系统数字字体。
- 合成图集使用独立 `akari-hero-compact` 目录；截图头像仍为占位，原生资产效果及最终视觉待用户确认。`typecheck:web` 与 106 项定向测试通过（1 跳过）；最终八主题 × 两种窗口的展开对齐、圆环选中联动、取消/焦点恢复、外层无溢出检查通过，32 张截图。数据泄漏守卫与差异空白检查通过。

### 2026-09-08 — 实际时间轴误判与控件状态修复

- 只读连接实际 Electron 后确认 15 分钟指标全部缺失；抽查本机缓存时间戳为 900323ms 等，原 `frame_at` 要求精确等于 900000ms，导致已有快照被丢弃。现接受前后 1 秒内最近观测，不用相邻分钟代替，推导版本升为 4；旧版本保留可读，通过正常刷新重算。
- 本地内存重算抽查中 4 场恢复 20 个成员的 15 分钟经济差；未修改原始缓存/数据库，未输出身份或对局内容。一次探针未设置 PYTHONPATH 失败，设置后重跑通过。Python 10 项测试通过，包含毫秒偏差与整分钟缺帧回归。
- 控件去掉按钮动画外圈、重复选中边框、分段选项残留分隔线及输入框发光，普通按钮/分段选项统一圆角；选择用轻底色与文字下划线，键盘焦点保留单层轮廓。仅限分析页，不修改上游全局主题。合成截图在本机 `akari-control-states`。
- 八主题鼠标选中、键盘焦点、分段切换与外层溢出检查通过，无页面脚本错误。首次选择到隐藏标签页的控件而超时，限定可见控件后重跑通过。数据泄漏守卫与差异空白检查通过；最终 `yarn build`（含 node/web 类型检查）通过，保留既有大分块/插件耗时警告；定向测试 106 通过、1 跳过。实际进程尚需加载新代码并执行正常刷新，不把内存重算当成当前界面已经更新。

### 2026-09-08 — 英雄池排版样板微调

- 按用户确认保留五分路与小圆环，去掉整块表格边框；统一标题、成员信息、头像与比例列。列内使用共享网格行高，展开更多英雄时同组后续条目保持对齐；不同成员出场范围仍各自显示，不补造数据。
- 英雄详情改为身份与取消操作在上、胜率及蓝红样本居中、指标对齐排列；窄窗口分三列/两列，保留选择、帮助与返回焦点。修改仅限 `ChampionView.vue`，合成截图在本机 `akari-hero-layout` 图集目录。图片请求仍主动阻止，因此截图头像为占位，不代表实际游戏资产效果。
- 用户进一步指出详情数字字体不协调：详情数值与胜率改用系统无衬线字体，数值 18px / 600 字重、统一行高与等宽数字，缩短标签到数值的距离；不引入字体文件，不影响数据精度。
- 最新字体版已复测八主题 × 两种窗口：展开后行对齐、选中/取消、焦点恢复和外层无横向溢出通过，生成 32 张默认及选中截图。`typecheck:web` 通过；定向测试 106 通过、1 跳过；数据泄漏守卫与差异空白检查通过；图集入口为本机 `akari-hero-layout/index.html`。
- 当前为待用户确认的局部样板，不声明原生 Electron 或完整视觉验收通过。

### 2026-09-08 — 剩余页面统一与点击后状态审查

- 按已认可方向覆盖组合、打野、事件结果、前期与胜负、同队偏差、指标对比、对局明细及设置：统一主题底色、标题、控件和数字层级。组合新增共用零线差值；事件按四类切换、蓝红逐项对照，完整明细改为纵向指标表，保留全部指标。英雄池仍为原来的分路分栏、小圆环与图例，仅改善说明、选择和焦点。
- 点击后检查发现并修复：固定页签遮挡详情、打野工具栏重叠、窄内容区资源列拥挤、窄布局指标说明入口消失、原生灰色取消按钮、设置放弃提示在屏外、英雄/散点/同队详情及新增成员的定位与焦点、隐藏高级筛选后看不到条件。散点改为主题底色内芯与分路色边框，避免浅色主题内部数字对比度不足。清理预览/执行失败增加就近反馈；收尾截图发现二次确认仍在小窗口屏外，已加入定位与焦点，并补测确认按钮完整位于抽屉底栏之上。不新增 IPC 或真实清理操作。
- 合成测试的点击矩阵覆盖八主题、1280×800 与 840×600，各生成 328 张交互状态截图；包含七组成员明细、蓝红拆分、被抓证据、三种组合详情、四组打野、四类事件、前期分档与证据、散点选中、英雄展开/说明/焦点、设置新增/无效/放弃/保存失败。另补充 56 项详情焦点、混合成员、组合七指标与分页检查；用纯内存 Storybook 场景检查清理预览/确认、保存成功、刷新中/取消/失败；不把模拟结果视作真实 IPC、刷新或数据删除验收。
- 检查过程中：预览服务因限额暂停而退出，恢复后重跑；首次测试选择了隐藏的打野说明入口，补齐窄布局可见入口并重跑。早期合成替补成员字面量触发数据泄漏守卫，改为从合成设置生成，未放宽守卫。交互页面无未捕获脚本错误，格式与数据泄漏检查通过。最终 `yarn build`（含 node/web 类型检查）通过，仍有既有大分块警告；最后一次修改后定向测试 106 通过、1 跳过。此前本轮全量测试 519 通过、1 跳过；数据泄漏守卫检查 1626 个文件通过，`git diff --check` 通过。
- 证据在本机临时目录 `akari-interactive-ui-0908`、`akari-interactive-ui-small-0908`、`akari-ui-workflows-0908`、`akari-ui-final-controls-0908`，均为合成输入，游戏图片请求主动阻止。精选图集保存在本机 `$CODEX_HOME/visualizations/2026/09/06/01a0781d-2e64-76c0-8013-1d3fccea3204/akari-interaction-review/index.html`。截图矩阵是自动化覆盖，不表示人工逐张批准；真实 Electron、原生 125% 缩放、真实清理/刷新、完整辅助技术验收仍未完成。既有历史数据缺失值限制不因本轮换样式而消失；不声明所有设计规范已最终验收。

### 2026-09-08 — 认可样式接入成员分路

- 用户认可五人同尺度样板后，将图表抽为 `MemberRoleComparison.vue`，正式成员分路与样板共用；完整指标、全部分路、蓝红拆分和被抓事件仍可展开。成员序号按配置固定，切页保留分路/指标选择；英雄池和其他页面本轮未改。
- 修复低窗口高度下柱子可见但姓名在首屏外的问题：紧凑布局在数值旁补姓名，完整姓名仍可在详情读取。八主题的常规与 840×600 页面均截图查看；操作验证指标/分路切换、明细、蓝红拆分、事件定位和切页状态。共用组件另验证零、缺失、极值、长名、1/6 样本、空筛选、键盘详情及焦点恢复，CSS 125% 检查不代替原生缩放。
- `yarn build` 通过（最终低高度姓名补丁前）；保留已有 `@reference` 和大包警告。全量默认并发测试出现更新模块一项 50ms 超时，未修改无关模块；该文件单独复跑 5 项通过，随后 `vitest run --maxWorkers=2` 全量 519 项通过、1 项跳过。最终补丁后重新检查渲染器类型、格式及数据泄漏。
- 合成证据位于本机临时目录 `akari-role-integration-9H2EsS`，浏览器无未捕获脚本异常；外部游戏图片请求主动阻止。真实 Electron、原生缩放和用户对正式页的最终视觉确认仍未完成，不代表其他页面或全部 UI 已验收。

### 2026-09-07 — 安装设计 Skill 与五人同尺度代码样板（视觉待确认）

- 个人目录安装并校验 `interface-design`、`design-critique`、`emil-design-eng`；来源分别固定于 `2f9be32`、`1f517b9`、`d23d7f8`，各目录保存来源与许可证。Design Critique 的 `argument-hint` 移入 metadata 以通过 Codex 校验；初次校验缺少 PyYAML，改用已有 Anaconda 环境后完成。未安装生成器或产品依赖。
- 只改独立 Storybook `Member Analysis/Audit / Design Sample`：共用零线的五人竖向经济差、参团率切换、列下成员与胜率区间、可展开详情；沿用原生标题/标签、主题和分路图标。修复负值标注位置、极值顶部裁切、长名称后的指标对齐及收起筛选后条件不可见。常规合成数据不变；另加 Boundary、Empty 场景。正式路由、英雄池、计算与 IPC 未修改。
- `typecheck:web` 通过；`vitest run member-analysis --maxWorkers=2` 为 105 项通过、1 项跳过；数据泄漏检查、格式及 diff 空白检查通过。浏览器检查分路/指标切换、键盘详情与焦点恢复、筛选空结果/清除、零值无条高、缺失无条、1/6 有效样本、长名称和极值。
- 八主题截图已生成并逐图查看，另查 1280×800、840×600 与 CSS 125% 缩放，外层无横向溢出。抽测姓名、次要文字及主图/胜率点的对比度达到本项目门槛，不代表完整可访问性认证。证据位于本机临时目录 `akari-skill-sample-0a9d93fe`；页面无未捕获脚本异常。外部游戏资源请求主动阻止，预览有资源加载及初始路由警告；预览服务另有仓库 ownership 警告，未修改全局 Git 信任设置。一次重拍等待网络空闲超时，改为等待页面元素后成功。
- 仍待用户确认外观；本次是浏览器组件样板，不是 Electron 原生/125% 原生缩放、完整加载失败状态或产品构建验收。历史缺失值限制不因换图得到修复，不将样板自动推广。

### 2026-09-07 — 成员识别优先的静态视觉探索

- 用户再次否定双图样板后，停止修改正式 UI，更新设计规范 11.8 的当前状态。使用内置 ImageGen 制作深色、浅色两张视觉概念稿，探索头像、五人成员区、字体层级、主比较图与选中成员详情的组合；没有把生成图片当作程序截图。
- 预览文件保留在本机 Codex generated_images 下，已在任务中展示，未成为产品资源。画像为示意，合成数字不涉及玩家数据；生成图的刻度位置、条长和字形仍有误差，不能作为统计或可访问性验收证据。正式落地必须由实际数据与代码绘图，不能直接把整张图嵌入页面。
- 本轮未改产品代码或英雄池，未运行应用检查；视觉方向仍待用户确认，不把任何一张稿视为已批准设计。

### 2026-09-07 — 先修方案，再制作图形比较样板

- 按用户要求，先在设计规范 11.8 明确主比较图与精确明细的结构，并在 11.6 暂停旧样板推广；随后只改独立 Storybook 样板。正式页面、数据计算和英雄池均未更改。
- 样板左侧为同尺度正负经济差横条，右侧为胜率与区间，完整数值移到下方展开；保留样本、单位、来源与历史缺失值限制。五人按设置顺序，未改造合成成绩来制造视觉差异。
- 检查：`typecheck:web`、数据泄漏检查及 `git diff --check` 通过；查看深浅主题与 840×600 截图，实际操作分路切换及展开明细。首次截图因预览服务未启动完成失败，服务就绪后重拍成功。截图位于本机临时目录 `akari-comparison-gEiRV6`。
- 仍为待用户确认的视觉方向，不是发布或八主题验收；未运行正式构建、全量测试、原生缩放及长名称极限检查。旧样板的被否定记录保留，不将新稿自动视作已获认可。

### 2026-09-07 — 独立成员分路视觉样板（待确认）

- 用户否定整体观感后，改为先做单页样板，不继续批量修改正式页面。新增 Storybook `Member Analysis/Audit / Design Sample`，正式路由与英雄池未变。
- 样板复用现有主题、筛选与聚合，合成数据保留五位成员的真实展示密度；以分路切换替代一次铺开全部分路，筛选按需展开，收紧标题区域，统一数值对齐。其他页面导航仅展示结构，不是假装已接通的按钮；全部指标组仍需确认后接入。
- 实际查看深浅主题与小窗口截图，验证分路切换、筛选展开、五行显示及无 NaN；中途预览出现的序号残留已移除。八主题已生成探索截图，最后修正后的复核仅深浅主题；不是八主题最终验收。数据泄漏与 diff 空白检查通过；截图在本机临时目录 `akari-sample-isG0p2`，不含真实玩家数据。
- 样板未替代产品，也未完成原生或边界验收；历史数据缺失值限制仍保留说明。先由用户判断视觉方向，再决定是否推广。
- 最终 `typecheck:web` 通过；本轮仅样板与 Storybook 接线，未重新运行全量测试或正式构建。

### 2026-09-07 — 打野、对局明细与交互细节打磨

- 保留英雄池结构。打野在窄窗口补回指标名称、压缩工具栏、保留非零营地直接标注并注明未列营地为零；缺失抓人值不再画成零点，10/15 分钟经济差取消连续走势连接，单个时间点缺失不隐藏另一点。
- 对局查询入口从每场重复按钮移到页头；对局展开使用完整时刻和单位、长名称换行、详情按宽度排列。被抓成员按钮直接定位事件区并转移键盘焦点；成员表与打野共用可关闭的指标说明，实测修复 Escape 未关闭弹层的问题。
- 设置保存抛出异常时保留输入并显示失败提示。修正格式工具调整导入顺序后导致的三组测试 YAML 加载失败，保留测试初始化先于组件导入。
- 检查：渲染器类型检查、成员分析 19 文件／106 项测试、数据泄漏检查通过。合成数据生成打野与展开对局的八主题截图，未发现整页横向溢出或页面脚本错误；复核深色对局、樱花打野、窄窗口被抓入口及 CSS 125% 放大，验证说明关闭后焦点恢复。截图位于本机临时目录 `akari-polish-review-MXGI50`，没有真实玩家数据。
- 仍不是全部视觉验收：八主题截图尚未逐图人工批准，CSS 放大不代替 Electron 原生缩放，最后补充的低高度间距压缩需原生复核。长名称极限、完整零/缺失边界、正式构建和真实 Electron 本轮未验收；已有 Python 缺失值问题仍需单独解决。

### 2026-09-07 — UI 首轮实施与英雄池反馈调整

- 用户授权开始实施；调整页面滚动与范围信息、成员默认指标、组合详情、前期分档表、散点重叠选择、事件分母和设置保存/放弃反馈。仍为首轮实现，不代表第 11 节全部完成。
- 按用户反馈撤回英雄池横条列表，恢复分路分栏、小圆环和直接图例；保留字号、选中提示、键盘操作及窄窗口换列改善，取消选择后不再自动选回首项。设计规范同步记录此取舍。
- 检查：`typecheck:web`、`vitest run member-analysis --maxWorkers=2`（19 文件／106 项）、数据泄漏检查和 `git diff --check` 通过。特性文件已格式化；首次格式命令额外包含一个错误的 story 路径并报错，实际 story 已由目录通配符覆盖。
- 合成数据英雄池生成八主题 1280×800 截图，无页面脚本错误或整页横向溢出；查看了深色、浅色、樱花及 840×600 预览，并验证选择/取消。截图保存在本机临时目录 `akari-hero-review-C6avQ5`，未使用玩家数据；外部头像请求被阻止，空图标不代表产品问题。
- 未覆盖：其余主题逐图人工复核、125% 缩放、完整边界状态、原生 Electron 和本轮正式构建；最终视觉仍待用户确认。此前记录的 Python 缺失值与分母问题未由本轮 UI 改动解决。

### 2026-09-07 — 补充统一设计语言

- 在设计规范 3.7–3.10 补充风格取舍、组件层级、图表共同外观及交互状态，覆盖本项目维护的页面；复用原有字体、尺寸、主题与统计规则，不增加第二套标准。
- 仅修改文档；逐页 UI 改版仍按第 11 节方案与截图确认流程执行。本次未改产品 UI、数据或依赖，未运行应用或声称完成视觉验收。

### 2026-09-06 — 代码审计与待确认的 UI 方案

- 范围：原生成员分析的 IPC、设置、刷新任务、存储、Python 计算、渲染器聚合及九个分析视图；对照上游页面和现行设计规范。不是全项目无缺陷声明。
- 已修复：过期刷新覆盖新状态、分页混合不同结果、部分设置保存重置扫描上限、运行中设置变更、刷新期间清理数据、退出时未等待采集结束、worker 非正常退出误报成功、未知数据库版本被改写、来源缺失被补成 SGP、错误信息及结构化身份字段脱敏、样本按时间排序、时间显示出现 `0:60`。固定分钟指标不再借用其他时刻的数据；已有分析需要重新刷新才能应用 Python 修复。
- 新增合成数据 Storybook 场景 `Member Analysis/Audit`，不连接真实 IPC；已浏览九个视图，生成八主题及 840×600 小窗口截图。截图来自浏览器组件预览，不代替完整 Electron、打包或人工视觉验收；外部图片请求被阻止，英雄图标为空不计为产品缺陷。产品 Vue/CSS 未调整。
- UI 待用户确认：完整方案统一维护在[设计规范第 11 节](design-spec.md#11-前端统一改版方案2026-09-06待用户确认)，替代本条原有的局部修改列表。
- 检查：使用已有 Node 24 运行环境，类型检查、正式构建、成员分析 19 个测试文件／101 项测试、Python 9 项测试、数据泄漏检查通过。构建仍报告 `@reference` 和大文件警告。系统 Node 23 的测试超时未作为产品失败依据；末轮全量测试为 91 文件／514 项通过，随后新增清理并发保护的接口测试 7 项及主进程类型检查通过。
- 未关闭项：Python 部分缺失字段及上下半区／20 分钟经济合计仍可能按零计入，需要连同可空字段、有效样本分母和旧数据兼容一起修复；清理文件失败后的恢复、迁移备份及文件路径边界需要补故障注入验证。原生真实采集、安装包和人工视觉确认未运行。
- 上游修改预算检查未通过：现有文档整理也计入上游修改，结果为 18 文件／836 行，超过 9 文件／350 行；没有调高预算或回退既有文档来制造通过。需明确维护文档与产品接入代码的统计范围后处理。

### 2026-09-06 — 项目文档与规则整理

- 缩短根/子目录 AGENTS、维护手册及 UI/数据 Skill；日常入口与旧记录分开，完整历史保留在单一归档中。
- 修正早期 worker 不可用、数据版本过时、禁止所有新文档、手动修改持久化数据、原始玩家 fixture 和过期全项目 PASS 等误导性要求。
- 初始技术设计标为历史；发布、设计、接口和工程文档明确分工。未修改程序、工作流、配置、依赖或玩家数据，未提交/发布。
- 验证：现行文档本地链接检查、五个项目 Skill 校验和修改的现行规则文件格式检查通过；`git diff --check` 通过（既有换行符提示）。仅文档变更，未运行应用测试/构建，不声称完成运行态或视觉验收。
- 精简：根/子 AGENTS、维护入口、维护手册、契约和两个设计 Skill 这七份日常文件由 2,853 行缩至约 406 行；完整历史转入归档，并非删除证据。原生包简短说明、上游 CHANGELOG、已标明退役的公告/原型说明和机器配置检查后保持原样。

### 2026-09-06 — 前端与数据可视化方案复核

- 复查原生九页及被抓统计的模板、筛选与聚合，结合上一轮合成截图和原版 TabbedPage。新发现散点同值覆盖、固定五人样本下同队偏差没有区分意义、区间图缺少清晰图例、构成图重复图例、事件汇总与宽表重复、低场数组合被自动隐藏。
- 在设计规范第 11 节集中列出页面结构、图形选择、逐页设计、交互、数据前置条件与验收；当前为待确认方案，未改产品 UI 或计算逻辑。
- 本次为代码／已有截图复核和文档修改，未重新运行应用、测试或声称完成边界状态视觉验收。
